import { WeatherObservation } from './weather-observation';

describe('WeatherObservation', () => {
  it('should create an instance', () => {
    expect(new WeatherObservation()).toBeTruthy();
  });
});
